import { makeStyles } from '@material-ui/core';

export default makeStyles({
  wrapper: {
    height: '100%',
    alignItems: 'center',
  },

  menuButton: {
    height: '100%',
    borderRadius: 5,
  },
});
